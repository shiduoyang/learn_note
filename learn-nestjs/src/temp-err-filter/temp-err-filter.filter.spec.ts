import { TempErrFilterFilter } from './temp-err-filter.filter';

describe('TempErrFilterFilter', () => {
  it('should be defined', () => {
    expect(new TempErrFilterFilter()).toBeDefined();
  });
});
